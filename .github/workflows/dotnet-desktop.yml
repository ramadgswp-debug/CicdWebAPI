name: .NET 8 WebAPI CI/CD to IIS

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: windows-latest

    steps:
      # 1️⃣ Checkout the code
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Setup .NET 8 SDK
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 8.0.x

      # 3️⃣ Restore dependencies
      - name: Restore dependencies
        run: dotnet restore

      # 4️⃣ Build the project
      - name: Build
        run: dotnet build --configuration Release --no-restore

      # 5️⃣ Run unit tests
      - name: Test
        run: dotnet test --no-build --verbosity normal

      # 6️⃣ Publish the project
      - name: Publish
        run: dotnet publish -c Release -o ./publish

      # 7️⃣ List published files (optional, for debug)
      - name: List published files
        run: dir ./publish /s

      # 8️⃣ Deploy to IIS server via PowerShell (overwrite existing files)
      - name: Deploy to IIS
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASS }}
          script: |
            # Stop App Pool to unlock files
            Import-Module WebAdministration
            Stop-WebAppPool -Name 'MyWebAPIAppPool'

            # Remove old files
            Remove-Item -Path 'C:\inetpub\wwwroot\MyWebAPI\*' -Recurse -Force

      # 9️⃣ Copy published files to Windows server
      - name: Copy published files
        uses: appleboy/scp-action@v0.1.6
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASS }}
          source: "./publish/*"
          target: "C:/inetpub/wwwroot/MyWebAPI"

      # 🔟 Start IIS App Pool
      - name: Restart App Pool
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASS }}
          script: |
            Import-Module WebAdministration
            Start-WebAppPool -Name 'MyWebAPIAppPool'

      # Optional: Restart IIS (if you want full reset)
      #- name: Restart IIS
      #  uses: appleboy/ssh-action@v0.1.6
      #  with:
      #    host: ${{ secrets.SERVER_HOST }}
      #    username: ${{ secrets.SERVER_USER }}
      #    password: ${{ secrets.SERVER_PASS }}
      #    script: iisreset
